version: 1
frontend:
  phases:
    preBuild:
      commands:
        - ls
    build:
      commands:
  artifacts:
    baseDirectory: /amazon-solution
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*