version: 1
frontend:
  phases:
    preBuild:
      commands:
        - node -v
        - npm -v
        - cd amazon-solution
        - amplify env checkout $BACKEND_ENV
        - npm ci
        - amplify env checkout $BACKEND_ENV
        - npm upgrade
        - npm outdated
    build:
      commands:
        - pwd
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*