version: 1
frontend:
  phases:
    preBuild:
      commands:
        - ls
        - node -v
        - npm -v
        - cd amazon-solution
        - ls
        - pwd
        - npm install
        - npm upgrade
        - npm outdated
    build:
      commands:
        - pwd
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*