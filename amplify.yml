version: 1
appRoute: 
  - ./amazon-solution
# env:
#   variables:
#     key: value
backend:
  phases:
    # IMPORTANT - Please verify your build commands
    build:
      commands:
        - amplifyPush --simple
        - npm i
        - npm run build
frontend:
  phases:
    preBuild:
      commands:
    build:
      commands:
  artifacts:
    baseDirectory: build
    files:
        - '**/*'
  cache:
    paths:
        - node_modules/**/*
