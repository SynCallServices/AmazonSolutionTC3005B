version: 1
appRoute: 
  - ./amazon-solution
# env:
#   variables:
#     key: value
frontend:
  phases:
    preBuild:
      commands:

    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
        - '**/*'
  cache:
    paths:
        - node_modules/**/*
