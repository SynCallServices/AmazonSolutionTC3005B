version: 1
frontend:
  phases:
    preBuild:
      commands:
        - node -v
        - npm -v
        - cd amazon-solution
        - pwd
        - npm install
    build:
      commands:
        - pwd
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*